### ToDo 


1. OrbitThreader: a short note on `trackBack()` and why the path is traced slightly backward
   before the forward pass,
2. OrbitThreader: a diagram of the two internal models:
   `ReferencePathModel` versus action-range registration,
